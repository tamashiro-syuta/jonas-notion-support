org: syuta
app: jonas-notion-support
service: jonas-notion-support
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs20.x

functions:
  api:
    handler: index.handler
    events:
      - httpApi:
          path: /
          method: get
