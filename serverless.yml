org: syuta
app: jonas-notion-support
service: jonas-notion-support
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs20.x
  region: ap-northeast-1

functions:
  api:
    handler: index.handler
    events:
      - httpApi:
          path: /
          method: get
